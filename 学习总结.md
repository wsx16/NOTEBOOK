# 大模型微调与量化学习总结

> 项目：Chinese-LLaMA-Alpaca-3 | 硬件：RTX 3050 Laptop（4GB VRAM）| 系统：Windows 11
> 学习时间：2025-2026 | 基础模型：Qwen1.5-1.8B

---

## 学习背景

| 项目 | 详情 |
|------|------|
| 项目名称 | Chinese-LLaMA-Alpaca-3 |
| 基础模型 | Qwen/Qwen1.5-1.8B（约3.5GB，适合4GB显存） |
| 硬件环境 | RTX 3050 Laptop，4GB VRAM，Windows 11 |
| 学习目标 | 掌握大模型微调（QLoRA SFT）和量化（GGUF/Ollama）的完整工业流程 |
| 学习结果 | 成功完成从数据准备 → 微调训练 → 模型合并 → 量化压缩 → 本地部署的全流程 |

---

## 整体流程图（ASCII）

```
原始数据 (my_test.json)
    │  Alpaca JSON 格式
    ▼
数据预处理 (build_dataset.py)
    │  Tokenize + Label Masking (IGNORE_INDEX=-100)
    │  缓存到磁盘
    ▼
QLoRA 微调训练 (run_clm_sft_with_peft.py)
    │  基础模型: Qwen1.5-1.8B (4bit量化加载)
    │  LoRA 适配器: rank=8, alpha=16
    │  可训练参数: 7,495,680 (0.4%)
    ▼
LoRA 权重 (adapter_model.safetensors)
    │
    ├── 直接 PEFT 推理 (加载 base + adapter)
    │
    └── 合并权重 (merge_and_unload)
            │  W_merged = W_base + B @ A * (alpha/r)
            ▼
        完整 HF 模型 (merged/)
            │
            ▼
        FP16 GGUF 转换 (convert_hf_to_gguf.py)
            │  3503.95 MiB
            ▼
        Q4_K_M 量化 (llama-quantize.exe)
            │  1155.67 MiB（压缩约67%）
            ▼
        Ollama 部署 (Modelfile-sft)
            │  ollama create + ollama run
            ▼
        本地 API / 对话界面
```

---

## 完整流程详解

### 第1步：Ollama 对话体验

**目的：** 建立感性认识，理解模型推理和部署的最终形态，再反向学习训练流程。

**做了什么：**
- 安装 Ollama，运行 `ollama run llama3` 进行对话体验
- 阅读 `scripts/ollama/Modelfile`，理解指令模板结构：

```
FROM ./model.gguf
TEMPLATE """<|start_header_id|>system<|end_header_id|>

{{ .System }}<|eot_id|><|start_header_id|>user<|end_header_id|>

{{ .Prompt }}<|eot_id|><|start_header_id|>assistant<|end_header_id|>

"""
PARAMETER stop "<|eot_id|>"
```

**关键理解：**
- **Modelfile 本质是配置文件**，告诉 Ollama 用哪个 GGUF 文件、用什么对话模板、设什么采样参数
- Llama-3 的特殊 token：`<|start_header_id|>`、`<|end_header_id|>`、`<|eot_id|>` 是模型的"句号"和"引号"
- 不同模型有不同的模板，**模板必须与训练时保持一致**，否则模型不知道对话结束了

---

### 第2步：数据格式理解

**目的：** 理解训练数据的组织方式，以及如何让模型只学习"答案"而不是"问题"。

**做了什么：**
- 学习 Alpaca JSON 格式（三字段结构）
- 理解 `IGNORE_INDEX=-100` 的 label masking 机制
- 手写自定义训练数据 `my_test.json`

**Alpaca JSON 格式示例：**

```json
[
  {
    "instruction": "什么是LoRA？",
    "input": "",
    "output": "LoRA（Low-Rank Adaptation）是一种参数高效微调方法..."
  },
  {
    "instruction": "请将以下英文翻译成中文",
    "input": "Hello, world!",
    "output": "你好，世界！"
  }
]
```

**Label Masking 原理（重点）：**

```
输入序列：[system prompt][user question][assistant answer]
标签序列：[  -100  -100 ][  -100  -100 ][  实际token  ]
```

- **只对 `output` 部分的 token 计算 loss**
- `instruction` 和 `input` 部分全部用 `-100` 屏蔽
- 原因：不希望模型"学会如何提问"，只学习"如何回答"
- PyTorch CrossEntropyLoss 自动忽略 label 为 `-100` 的位置

**`input` 字段的作用：**
- `input` 为空时：模型只看 `instruction` 回答
- `input` 非空时：`instruction` 是任务说明，`input` 是具体内容
- 例：instruction="翻译成中文"，input="Hello"，output="你好"

---

### 第3步：量化实验

**目的：** 理解量化压缩的原理和效果，体验从 HF 格式到 GGUF 格式再到量化模型的转换流程。

**做了什么：**

1. 下载 `Qwen/Qwen1.5-1.8B`（约3.5GB FP16权重）
2. 用 `llama.cpp/convert_hf_to_gguf.py` 转为 FP16 GGUF
3. 用 `llama-quantize.exe` 做 Q4_K_M 量化
4. 对比文件大小，跑推理验证质量

**量化过程：**

```bash
# 步骤1：HF 格式 → FP16 GGUF
python convert_hf_to_gguf.py Qwen1.5-1.8B/ \
    --outfile qwen1.5-1.8b-f16.gguf \
    --outtype f16

# 步骤2：FP16 GGUF → Q4_K_M 量化
llama-quantize.exe qwen1.5-1.8b-f16.gguf qwen1.5-1.8b-q4km.gguf q4_k_m
```

**量化结果对比：**

| 格式 | 文件大小 | 精度 | 适用场景 |
|------|---------|------|---------|
| HF FP16 | ~3.5 GB | 最高 | 训练/微调 |
| GGUF FP16 | 3503.95 MiB | 最高 | llama.cpp推理 |
| GGUF Q8_0 | ~1.8 GB | 很高 | 高质量推理 |
| **GGUF Q4_K_M** | **1155.67 MiB** | **较高** | **平衡推荐** |
| GGUF Q4_0 | ~1.0 GB | 中等 | 极限压缩 |

**量化原理（面试必考）：**
- **FP16**：每个权重 16 bits，范围大精度高
- **INT8/Q8**：每个权重 8 bits，压缩2倍，精度损失极小
- **Q4_K_M**：每组 32 个权重共用一套缩放因子（K=使用K-quant，M=medium），平均约4.5 bits/权重
- 量化不改变模型结构，只改变权重存储精度
- **推理时会反量化**：INT4 → FP16/FP32 再做矩阵乘法

---

### 第4步：QLoRA SFT 微调（核心步骤）

**目的：** 用少量自定义数据微调基础模型，使其具备特定领域知识或对话风格。

**关键训练配置：**

```bash
# run_sft.sh 核心参数（4GB VRAM 适配版）
model_name_or_path="Qwen/Qwen1.5-1.8B"
dataset_dir="./data"
lora_rank=8
lora_alpha=16
max_seq_length=128
gradient_accumulation_steps=16
load_in_kbits=4              # QLoRA 4bit量化加载
trainable="q_proj,v_proj"    # 只对这些层加LoRA
preprocessing_num_workers=1  # Windows下必须设为1
```

**训练结果：**

| 指标 | 值 |
|------|-----|
| 可训练参数 | 7,495,680（占总参数 **0.4%**） |
| 总参数 | ~1.8B |
| train_loss | 3.3103 |
| eval_loss | 2.4973 |
| **perplexity** | **12.15** |
| 训练时间 | 17秒（27条数据，3个epoch） |
| 峰值显存 | ~3.8GB |

**LoRA 原理（重点）：**

```
原始权重矩阵 W (d×k)  →  冻结，不更新

新增低秩分解：
  A (r×k)  ← 随机初始化
  B (d×r)  ← 初始化为0

前向传播：
  h = W·x + (B·A)·x · (alpha/r)

训练时只更新 A 和 B
```

- `r=8` 时，LoRA 参数 = `2 × d × r`，比全参数少 `k/r` 倍
- `alpha/r` 是缩放因子，控制 LoRA 对原始权重的影响强度
- 合并时：`W_merged = W + B @ A * (alpha/r)`

---

### 第5步：合并 + 部署

**目的：** 将训练好的 LoRA 适配器合并回基础模型，然后量化部署。

**合并方法：**

```python
# 方法1：PEFT 一行合并（内存友好）
from peft import PeftModel
model = AutoModelForCausalLM.from_pretrained(base_model_path)
model = PeftModel.from_pretrained(model, lora_adapter_path)
merged_model = model.merge_and_unload()
merged_model.save_pretrained("./merged")

# 方法2：项目提供的低内存合并脚本
python merge_llama3_with_chinese_lora_low_mem.py \
    --base_model ./Qwen1.5-1.8B \
    --lora_model ./output/sft \
    --output_dir ./merged
```

**部署流程：**

```bash
# 转 GGUF
python convert_hf_to_gguf.py ./merged --outfile merged-f16.gguf --outtype f16

# 量化
llama-quantize.exe merged-f16.gguf merged-q4km.gguf q4_k_m

# 写 Modelfile-sft
# FROM ./merged-q4km.gguf
# TEMPLATE ... (Llama-3/Qwen 模板)

# 创建并运行
ollama create my-sft-model -f Modelfile-sft
ollama run my-sft-model
```

**测试结果与分析：**
- 问"什么是LoRA"，模型回答了 **LoRaWAN**（无线通信协议）而非机器学习的 LoRA
- 根本原因：**训练数据只有27条，严重不足**
- 工业实践中通常需要 **1000条以上**高质量数据才能有明显效果
- 这证明了微调效果高度依赖数据质量和数量

---

## 踩坑记录：10个错误及根因分析

### 错误1：bash run_sft.sh 无法运行

**现象：** `bash: command not found` 或脚本中 `torchrun` 路径错误

**根因：** Windows 没有原生 bash，脚本中路径分隔符和命令格式是 Linux 风格

**解决方案：**
```bat
:: 创建 run_sft.bat
@echo off
python -m torch.distributed.run --nnodes=1 --nproc_per_node=1 ^
    run_clm_sft_with_peft.py ^
    --model_name_or_path "D:\models\Qwen1.5-1.8B" ^
    --load_in_kbits 4 ...
```
**经验：** Windows 环境下跑 ML 项目需要将 shell 脚本改写为 .bat 或直接用 Python 调用

---

### 错误2：TensorFlow DLL 加载错误

**现象：** `ImportError: DLL load failed while importing _pywrap_tensorflow_internal`

**根因：** 安装了 TensorFlow 但版本与 CUDA 不匹配，且项目并不需要 TF

**解决方案：**
```bash
pip uninstall tensorflow tensorflow-intel
```
**经验：** ML 项目中应保持环境干净，不安装不需要的框架；TF 和 PyTorch 共存经常有冲突

---

### 错误3：transformers 5.x 版本删除了接口

**现象：** `ImportError: cannot import name 'send_example_telemetry' from 'transformers'`

**根因：** transformers 5.x 大版本升级删除了部分旧接口，项目代码依赖 4.x API

**解决方案：**
```bash
pip install transformers==4.46.0
```
**经验：** 使用开源项目时，**优先查看 README 中的版本要求**；大版本升级往往有 breaking change

---

### 错误4：peft 版本与 transformers 不匹配

**现象：** `ImportError: cannot import name 'EncoderDecoderCache' from 'transformers'`

**根因：** 新版 peft 依赖新版 transformers 的 EncoderDecoderCache 类，但 transformers 降级后该类消失

**解决方案：** 将 transformers 固定在 4.46.0 后，配套安装 peft 0.13.x，依赖链自动对齐

**经验：** Python 依赖管理复杂，`pip install` 不会自动降级已安装包；建议用 `pip install package==version` 明确指定

---

### 错误5：torch 是 CPU 版本，无法使用 GPU

**现象：** `torch.cuda.is_available()` 返回 False，训练极慢

**根因：** `pip install torch` 默认安装 CPU 版本（`torch-2.6.0+cpu`）

**解决方案：**
```bash
# 先卸载 CPU 版本
pip uninstall torch torchvision torchaudio

# 安装 CUDA 12.1 版本
pip install torch torchvision torchaudio \
    --index-url https://download.pytorch.org/whl/cu121
```
**经验：** 安装 PyTorch 必须去官网 [pytorch.org](https://pytorch.org) 选择正确的 CUDA 版本，**不能直接 pip install**

---

### 错误6：数据格式错误（JSON Array vs JSONL）

**现象：** `ValueError: Expecting value: line 1 column 1 (char 0)` 或数据加载失败

**根因：** 项目的 `build_dataset.py` 期望 JSONL 格式（每行一个 JSON 对象），而准备的数据是 JSON Array 格式（`[{...}, {...}]`）

**解决方案：**
```python
import json

# 读取 JSON Array
with open("my_test.json") as f:
    data = json.load(f)

# 转换为 JSONL
with open("my_test.jsonl", "w") as f:
    for item in data:
        f.write(json.dumps(item, ensure_ascii=False) + "\n")
```
**经验：** 使用数据集前，先用小样本测试数据加载是否正常；JSON 和 JSONL 是两种完全不同的格式

---

### 错误7：Windows MAX_PATH 路径过长

**现象：** `FileNotFoundError` 或 `OSError: [Errno 36] File name too long`

**根因：** Windows 默认路径长度上限为 260 字符；缓存目录路径 + 长文件名超过限制

**解决方案：**
- 将数据文件重命名为短名称（如 `ruozhiba.json` → `rzb.json`）
- 输出目录路径尽量短（如 `D:\sft_out` 而非嵌套多层目录）
- 或在注册表/组策略中开启长路径支持

**经验：** Windows 路径问题是跑 ML 项目的常见坑；建议将所有模型和数据放在根目录附近（如 `D:\models\`）

---

### 错误8：多进程加载数据导致 CUDA 内存不足

**现象：** 启动8个子进程预处理数据时，OOM（Out of Memory）崩溃

**根因：** `preprocessing_num_workers=8` 会启动8个进程，每个进程都会初始化 CUDA 上下文，4GB 显存撑不住

**解决方案：**
```python
# 在训练参数中设置
--preprocessing_num_workers 1
```
**经验：** 4GB 显存机器，数据预处理进程数必须设为1；大显存机器（24GB+）才适合多进程并行

---

### 错误9：peft 新版 prepare_model_for_kbit_training 导致 OOM

**现象：** 调用 `prepare_model_for_kbit_training()` 后，模型显存占用暴增，OOM

**根因：** 新版 peft 的该函数会将**所有参数**转换为 float32（为了计算梯度），但 1.8B 模型的 float32 权重远超 4GB

**解决方案（修改 run_clm_sft_with_peft.py）：**
```python
# 错误方式（新版peft默认行为）
model = prepare_model_for_kbit_training(model)  # 全部转fp32 → OOM

# 正确方式：只转 LayerNorm 层为 fp32
for name, param in model.named_parameters():
    if "norm" in name.lower():
        param.data = param.data.to(torch.float32)
```
**经验：** QLoRA 的关键是"大部分权重保持4bit，只有归一化层用fp32"；peft 版本不同行为不同，需要仔细阅读源码

---

### 错误10：词表大小不匹配导致 MemoryError

**现象：** 保存模型时 `MemoryError`，或加载时 `RuntimeError: size mismatch`

**根因：** 项目原本为 Llama-3 设计，会在 tokenizer 上添加中文 token 并 resize embedding；但 Qwen1.5 的词表已包含中文，resize 后词表大小不一致导致内存申请过大

**解决方案：**
```python
# 只在 tokenizer 比模型 embedding 更大时才 resize
if len(tokenizer) > model.get_input_embeddings().weight.shape[0]:
    model.resize_token_embeddings(len(tokenizer))
```
**经验：** 换用不同基础模型时，要检查词表大小是否已兼容，不要盲目 resize

---

## 重点内容汇总

### LoRA / QLoRA 核心概念

| 概念 | 说明 |
|------|------|
| **LoRA rank (r)** | 低秩矩阵的秩，越大参数越多效果越强，但显存占用也越大 |
| **alpha** | 缩放因子，`alpha/r` 控制 LoRA 对原始权重的影响强度 |
| **QLoRA** | 在4bit量化的基础模型上做LoRA，显存需求降低约75% |
| **trainable layers** | 通常选 `q_proj, v_proj`（注意力的Q、V投影），效果最好 |
| **modules_to_save** | `embed_tokens, lm_head` 用全精度训练，因为词表可能扩展 |

### 量化核心概念

| 概念 | 说明 |
|------|------|
| **GGUF 格式** | llama.cpp 使用的量化格式，单文件包含所有权重+元数据 |
| **Q4_K_M** | 4bit量化，K-quant方法，M=medium精度，**最常用** |
| **困惑度 (Perplexity)** | 语言模型质量指标，越低越好；量化后通常增加0.1-0.3 |
| **KV Cache** | 推理时缓存注意力的K、V矩阵，避免重复计算 |

### 关键数字（面试必备）

- LoRA 可训练参数比例：**0.4%**（相比全参数微调）
- Q4_K_M 压缩比：**约3倍**（3.5GB → 1.1GB）
- QLoRA 显存节省：**约75%**（相比全精度微调）
- 有效微调最少数据量：**1000条**（实际工业标准）
- 本次实验数据量：**27条**（效果不佳，仅用于流程验证）

---

## 面试官会考什么：20个常见问题及答案要点

### 基础概念类

**Q1：什么是 LoRA？为什么它能减少训练参数？**

答案要点：
- LoRA（Low-Rank Adaptation）假设权重更新矩阵 ΔW 是低秩的
- 将 ΔW（d×k）分解为 B（d×r）× A（r×k），其中 r << min(d,k)
- 参数量从 d×k 降为 r×(d+k)，当 r=8, d=k=4096 时，参数量减少约 256 倍
- **训练时冻结原始权重 W，只更新 A 和 B**

---

**Q2：QLoRA 和 LoRA 有什么区别？**

答案要点：
- LoRA：基础模型用 FP16 加载，显存占用仍然较大
- **QLoRA**：基础模型用 **4bit NormalFloat (NF4)** 加载，显存减少约75%
- QLoRA 还引入了双量化（Double Quantization）和分页优化器（Paged Optimizer）
- 代价：训练速度比 LoRA 慢约10-20%，因为有反量化开销

---

**Q3：什么是 IGNORE_INDEX=-100？为什么要这样做？**

答案要点：
- PyTorch 的 `CrossEntropyLoss` 默认忽略 label=-100 的位置
- 在 SFT 中，将 prompt（instruction+input）部分的 label 全设为 -100
- **只对 output（assistant回答）部分计算损失**
- 这样模型学习"如何回答"而不是"如何提问"
- 如果不这样做，模型会过度学习提问方式，回答质量下降

---

**Q4：Alpaca 数据格式的三个字段各有什么用途？**

答案要点：
- `instruction`：任务描述或问题，必填
- `input`：任务的具体输入内容，可为空字符串
- `output`：期望的模型回答，必填，这部分计算损失
- 当 input 非空时，模型接受 instruction+input 的组合输入

---

**Q5：什么是困惑度（Perplexity）？如何判断好坏？**

答案要点：
- Perplexity = exp(平均交叉熵损失)，衡量模型对测试集的"不确定程度"
- **越低越好**：PPL=1 表示完美预测，PPL=∞ 表示完全随机
- 参考值：GPT-4 在 WikiText 上约 5-10，本次实验 12.15 属于正常范围
- 注意：PPL 是相对值，不同数据集、不同 tokenizer 的 PPL 不可直接比较

---

### 工程实践类

**Q6：为什么 4GB 显存能跑 1.8B 模型的微调？**

答案要点：
1. **4bit 量化加载**：1.8B × 2B(FP16) = 3.6GB → 1.8B × 0.5B(INT4) ≈ 0.9GB
2. **只训练0.4%的参数**（LoRA），大部分参数不需要梯度
3. **梯度累积**（gradient_accumulation=16）：小 batch size，显存峰值低
4. **gradient checkpointing**：用时间换空间，重新计算中间激活值
5. 综合以上，峰值显存约3.8GB，刚好在4GB内

---

**Q7：梯度累积（gradient_accumulation_steps）是什么原理？**

答案要点：
- 正常训练：每个 batch 计算梯度后立即更新参数
- 梯度累积：连续 N 个小 batch 的梯度累加后才更新一次
- 效果等价于 batch_size × N 的大 batch，但显存只需要小 batch 的量
- 代价：N 步后才更新一次，训练速度可能略慢

---

**Q8：LoRA 合并的数学公式是什么？合并后精度如何？**

答案要点：
- 公式：`W_merged = W_base + B @ A × (alpha / r)`
- 合并是**无损的**（在相同精度下），推理结果与加载 base+adapter 完全一致
- 合并的目的：部署时只需加载一个模型，避免每次推理都加载两个模型

---

**Q9：GGUF 量化有哪些格式？如何选择？**

答案要点：
- **Q4_K_M**：4bit K-quant，最推荐，平衡质量与大小（本次使用）
- **Q8_0**：8bit，接近原始精度，文件约为FP16的一半
- **Q5_K_M**：5bit，Q4_K_M 和 Q8_0 之间的折中
- **Q4_0**：最小，但质量损失明显，不推荐
- 选择原则：有条件用 Q8_0，内存有限用 Q4_K_M，极限压缩用 Q4_0

---

**Q10：为什么微调后模型回答了"LoRaWAN"而不是"LoRA"？**

答案要点：
- **根本原因：训练数据太少**（27条），模型没有足够见过 LoRA 相关知识
- 基础模型（Qwen1.5-1.8B）本身知识中 LoRaWAN 可能比 ML LoRA 更常见
- 微调没有改变基础模型对"LoRA"词汇的底层理解
- 解决方案：增加1000+条关于 ML LoRA 的高质量训练数据

---

### 进阶理论类

**Q11：LoRA 应该加在哪些层上？为什么？**

答案要点：
- 原始论文推荐：`q_proj` 和 `v_proj`（注意力的 Query 和 Value 投影）
- 也可以加：`k_proj`, `o_proj`, `gate_proj`, `up_proj`, `down_proj`
- 加的层越多，参数越多，效果越好，但显存占用也越大
- **经验：** 4GB 显存只加 q_proj+v_proj；充足显存可以加全部注意力层

---

**Q12：SFT、RLHF、DPO 有什么区别？**

答案要点：
- **SFT（Supervised Fine-Tuning）**：用人工标注的正确回答训练，本次实验使用
- **RLHF（Reinforcement Learning from Human Feedback）**：先训练奖励模型，再用RL优化
- **DPO（Direct Preference Optimization）**：直接从偏好数据（好回答vs坏回答）训练，无需奖励模型
- ChatGPT 使用 RLHF；Llama-3 Instruct 使用 SFT + RLHF/DPO；本次只做 SFT

---

**Q13：什么是 Flash Attention？有什么好处？**

答案要点：
- 传统 Attention 需要将完整的 N×N 注意力矩阵存在 HBM（显存）中
- Flash Attention 通过分块计算，**避免将整个注意力矩阵存入显存**
- 显存占用从 O(N²) 降为 O(N)，速度提升2-4倍
- 训练超长序列（>2048 tokens）时几乎必须启用

---

**Q14：int4 量化的原理是什么？推理时如何处理？**

答案要点：
- 将一组连续权重（如32个）映射到 0-15 的整数范围
- 存储：每个权重4bits，外加每组的 scale 和 zero_point（FP16）
- **推理时反量化**：`float_val = int_val × scale + zero_point`，再做矩阵乘法
- 量化误差来自：将连续值映射到离散空间时的舍入误差

---

**Q15：为什么要用 LayerNorm 层保持 FP32？**

答案要点：
- LayerNorm 需要计算均值和方差，对数值精度非常敏感
- 4bit 精度的 LayerNorm 会导致训练不稳定（梯度爆炸/消失）
- **QLoRA 的标准做法：权重用 4bit，但 LayerNorm（和部分归一化层）用 FP32**
- 这是训练稳定性和显存效率之间的折中

---

### 部署与优化类

**Q16：Ollama 的 Modelfile 必须包含哪些要素？**

答案要点：
- `FROM`：指定 GGUF 文件路径（必填）
- `TEMPLATE`：对话模板，**必须与训练时的模板完全一致**（非常重要）
- `PARAMETER stop`：停止符，告诉模型何时结束回答
- 可选：`SYSTEM`（系统提示）、`PARAMETER temperature`（采样温度）等

---

**Q17：如何评估微调效果？除了 loss 还有哪些指标？**

答案要点：
- **定量指标**：Perplexity、BLEU（翻译）、ROUGE（摘要）、准确率（分类）
- **定性评估**：人工评测，打分（1-5分）
- **基准测试**：C-Eval（中文）、MMLU（英文）、HumanEval（代码）
- **实际使用**：在目标场景下测试，看是否达到业务需求

---

**Q18：全参数微调和 LoRA 微调在什么场景下各自更适合？**

答案要点：
- **LoRA 适合**：数据量少（<10万条）、显存有限、需要快速迭代、多任务切换
- **全参数适合**：数据量充足（>100万条）、显存充足（>80GB）、需要深度改变模型能力
- **实际工程中**：95%的场景用 LoRA 就够了，全参数微调成本极高

---

**Q19：如何防止微调时的灾难性遗忘？**

答案要点：
- **LoRA 本身就是防遗忘的**：原始权重冻结，只增量学习
- 全参数微调时：混入通用数据（replay），防止模型忘记原有知识
- 使用较小的学习率（1e-5 以下）
- 训练轮数不要太多（通常1-3 epochs）
- 监控基准测试分数，发现遗忘及时停止

---

**Q20：生产环境中如何服务多个用户的 LLM？**

答案要点：
- **Ollama**：单机本地部署，适合个人/小团队
- **vLLM**：高性能推理框架，支持 PagedAttention，吞吐量比 HF 高10-20倍
- **TGI（Text Generation Inference）**：HuggingFace 出品，支持流式输出
- **关键优化**：连续批处理（Continuous Batching）、KV Cache 管理、量化
- **多卡部署**：Tensor Parallelism（张量并行）分摊单卡压力

---

## 实际工作中的应用建议

### 场景1：企业内部知识库问答

**需求：** 让模型回答公司特有的产品知识、内部流程

**方案：**
```
方案A（推荐）：RAG（检索增强生成）
  - 不需要微调，直接将知识库建成向量数据库
  - 推理时检索相关文档，塞入 context
  - 优点：知识可实时更新，成本低
  - 工具：LangChain + ChromaDB + Ollama

方案B：SFT 微调
  - 适合知识量小（<1000条）且相对固定的场景
  - 需要持续维护训练数据
  - 本次实验的做法
```

---

### 场景2：客服机器人微调

**需求：** 让模型学会公司的客服话术和回答风格

**最佳实践：**
1. **数据准备**：收集1000-5000条历史优质客服对话
2. **数据清洗**：去除重复、低质量对话；标准化格式
3. **基础模型选择**：7B 模型效果最好；资源有限用 1.8B
4. **训练参数**：rank=16, alpha=32, max_seq_length=512, epochs=3
5. **评估**：人工抽查100条，业务同事评分
6. **部署**：vLLM 服务化，前端接 API

---

### 场景3：代码助手微调

**需求：** 让模型专注于特定编程语言或框架的代码生成

**数据来源：**
- GitHub 上的高 star 项目代码
- Stack Overflow 的问答对
- 公司内部代码规范示例

**注意事项：**
- 代码数据中注意**许可证问题**
- 使用 `starcoder` 或 `deepseek-coder` 作为基础模型，它们已经在代码上预训练
- 序列长度设置更长（1024-2048），代码通常较长

---

### 场景4：低资源环境部署（类似本实验）

**硬件：** 4-8GB 显存，消费级 GPU

**最优路径：**
```
选择 1.8B-7B 模型
    ↓
QLoRA 微调（load_in_kbits=4）
    ↓
LoRA 合并
    ↓
Q4_K_M 量化
    ↓
Ollama 部署
```

**关键参数配置（4GB显存）：**
```bash
model: Qwen1.5-1.8B 或 Qwen2-1.5B
lora_rank: 8
max_seq_length: 128-256
batch_size: 1
gradient_accumulation: 16
preprocessing_num_workers: 1
```

---

### 实用工具清单

| 工具 | 用途 | 备注 |
|------|------|------|
| HuggingFace Transformers | 模型加载/训练 | 核心库 |
| PEFT | LoRA 实现 | HF 官方 |
| bitsandbytes | 量化训练 | Linux 原生更稳定 |
| llama.cpp | GGUF 转换/推理 | CPU推理很快 |
| Ollama | 本地模型部署 | 最简单 |
| vLLM | 生产级推理 | 高并发 |
| LangChain | RAG 应用开发 | 生态丰富 |
| Weights & Biases | 训练监控 | 推荐使用 |

---

## 总结

通过本次实验，完成了大模型微调的完整工业流程：

1. **理解了工具链**：Ollama → HuggingFace → llama.cpp 的关系
2. **掌握了核心原理**：LoRA 低秩分解、QLoRA 量化微调、GGUF 格式量化
3. **解决了10个工程问题**：Windows 环境、版本冲突、显存限制等实际挑战
4. **建立了量的概念**：0.4% 参数、3倍压缩、27条数据不够用

**最重要的一课：** 数据质量和数量是微调效果的决定性因素，再好的技术流程也无法弥补数据的不足。工业级微调的核心工作量在于**数据工程**，而非模型架构。

---

*本文档生成于 2026-03-21，基于 Chinese-LLaMA-Alpaca-3 项目实验总结*
