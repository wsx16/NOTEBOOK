# 基于 STM32U5 的多协议物联网智能终端

## 一、项目概述

本项目是一个基于 **STM32U575RITx**（Cortex-M33）的嵌入式物联网终端系统，集成了 **Modbus RTU 工业通信**、**MQTT 云端通信**、**指纹识别**和**触摸屏 GUI** 四大功能模块。采用 **FreeRTOS** 实时操作系统进行多任务调度，实现了本地设备控制与远程物联网通信的统一管理。

## 二、系统架构（自顶向下）

整个系统可以分为三层：

| 层级 | 组成 |
|------|------|
| **应用层** | LVGL 触摸屏界面 / Modbus 指令解析调度 |
| **通信层** | USART1-Modbus / UART5-MQTT / USART3-指纹 |
| **系统层** | FreeRTOS 任务调度 / STM32 HAL 驱动 |

## 三、核心设计思路：事件驱动 + 消息队列解耦

这是本项目**最核心的架构设计**。所有外设控制都统一抽象为 **Modbus 指令包**（`modbus_pack_t`），无论指令来源是：

1. **外部串口**：上位机通过 RS-485 发送 Modbus RTU 帧
2. **触摸屏 GUI**：用户点击按钮，回调函数构造指令包
3. **MQTT 云端**：远程下发控制命令

都通过 **FreeRTOS 消息队列**（`xQueueSend`）统一投递到 `modbus_parse_task` 任务中处理。

这样做的好处是：

- **解耦**：UI 层、通信层、控制层彼此独立，互不阻塞
- **线程安全**：中断上下文使用 `xQueueSendFromISR`，任务上下文使用 `xQueueSend`，避免竞态条件
- **可扩展**：新增控制来源只需构造指令包投入队列即可

## 四、FreeRTOS 多任务设计

系统运行 **3 个任务**，按优先级从高到低：

| 任务名 | 优先级 | 栈大小 | 职责 |
|--------|--------|--------|------|
| `modbus_parse_task` | Normal | 4KB | 从队列取指令包，执行外设控制（LED/蜂鸣器/WiFi/指纹） |
| `F0_LogTask` | BelowNormal | 1KB | 通过 StreamBuffer 接收指纹模块的异步日志并打印 |
| `lvgl` | Low | 2KB | 每 5ms 调用 `lv_timer_handler()` 刷新 GUI |

**IPC 机制**：

- **消息队列**（4 个槽位）：传递 `modbus_pack_t` 指令包
- **流缓冲区**（2048 字节）：指纹模块 USART3 的异步日志转发

## 五、通信协议实现

### 5.1 Modbus RTU（USART1）

自定义精简 Modbus 帧格式：

| 设备号 (1B) | 功能码 (1B) | 参数 (0~1B) | CRC16 (2B) |
|-------------|-------------|-------------|------------|
| 0x01 (LED)  | 0x01 (ON)   | 可选        | 小端序     |

- 帧长仅 4~5 字节，适合低带宽场景
- CRC16 采用标准 Modbus 多项式 `0xA001`
- 接收采用 **HAL 空闲中断**（`HAL_UARTEx_ReceiveToIdle_IT`），一帧数据到齐后触发回调
- 回调中解析、校验、组包后通过 `xQueueSendFromISR` 投递到任务

### 5.2 MQTT over WiFi（UART5 + ESP8266）

- 通过 **AT 指令集**驱动 ESP8266 模块
- 完整实现了：WiFi 连接（含自动重连与 IP 有效性检测）-> MQTT 用户配置 -> 连接 Broker -> 订阅/发布主题
- `send_AT_Cmd()` 封装了发送-等待-超时-匹配的通用 AT 指令交互逻辑
- MQTT Broker：`broker.emqx.io:1883`（公共测试服务器）

### 5.3 指纹模块（USART3）

- 与外挂 STM32F0 指纹控制板通信
- 支持录入（`'1'`）、识别（`'2'`）、删除（`'3'` + ID）三种操作
- 指纹模块的返回数据通过 **StreamBuffer** 异步转给 `F0_LogTask` 打印日志，避免阻塞主任务

## 六、触摸屏 GUI（LVGL 8.3.11）

- **硬件**：ILI9341 2.8寸 TFT（240x320，SPI 接口）+ FT6336 电容触摸（I2C 接口）
- **显示刷新**：TIM6 提供 1ms 中断驱动 `lv_tick_inc(1)`，任务中 5ms 周期调用 `lv_timer_handler()`

GUI 分为 **3 个屏幕**：

| 屏幕 | 功能 |
|------|------|
| 主屏幕 | 5 个功能按钮（LED 开关、蜂鸣器开关、MQTT 连接、WiFi 配置、指纹入口） |
| 指纹屏幕 | 录入 / 识别 / 删除三个操作按钮 + 返回 |
| 指纹删除屏幕 | 数字键盘输入 ID（0~9）+ 确认 / 取消 |

**亮点**：WiFi 配置窗口是运行时动态创建的弹窗，包含 SSID/密码输入框和虚拟键盘，用户输入后构造 `DEV_SYSTEM + FUNC_SAVE_WIFI` 指令包发到队列，由 `modbus_parse_task` 执行实际的 AT 指令连接。这体现了 **UI 与业务逻辑的完全解耦**。

## 七、OTA 固件升级（预留）

- 应用程序起始地址偏移到 `0x0800A000`，低地址留给 Bootloader
- Flash 末尾 `0x080FE000` 扇区存储版本信息
- 集成 MD5 校验算法用于固件完整性验证

## 八、技术亮点总结

1. **统一指令模型**：所有控制源（串口/GUI/云端）统一为 Modbus 指令包，通过消息队列解耦，架构清晰
2. **中断+RTOS 协作**：UART 空闲中断完成帧接收，ISR 中仅做入队操作，业务逻辑在任务上下文执行，兼顾实时性与安全性
3. **多协议并行**：Modbus RTU、MQTT、指纹三路串口同时工作，互不干扰
4. **GUI 与业务解耦**：触摸屏回调不直接操作硬件，而是构造指令包投入队列，保持了单一职责原则
