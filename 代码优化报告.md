# 代码优化报告

## 概述

本次优化针对项目中发现的 **22 项问题**（7 个严重 Bug、5 个数据库问题、4 个性能问题、6 个代码质量/架构问题）进行了全面修复，并新增了摄像头接入、数据搜索等功能。涉及文件：`db.py`、`main.py`、`migrate_to_db.py`。

---

## 一、`db.py` 修复（5 项）

### 1.1 修复连接泄漏

**问题：** `_get_conn()` 创建的 SQLite 连接从未被显式关闭。Python `sqlite3` 的 `with conn:` 语境管理器只负责提交/回滚事务，**不关闭连接**，每次调用 `insert` / `fetch` 函数都会积累一个打开的文件句柄。

```python
# ❌ 修复前：连接永不关闭
def insert_rush_event(event, video_name=""):
    with _get_conn() as conn:    # with 只管事务，不管关闭
        conn.execute(...)
        conn.commit()

# ✅ 修复后：closing() 确保退出 with 块时关闭连接
from contextlib import closing

def insert_rush_event(event, video_name=""):
    with closing(_get_conn()) as conn:
        conn.execute(...)
        conn.commit()
```

**影响范围：** `init_db`、`insert_plate_records`、`insert_rush_event`、`fetch_rush_events` 全部改用 `closing()`。

---

### 1.2 新增唯一约束防止重复写入

**问题：** 两张表均无 UNIQUE 约束，重复处理同一视频会写入完全相同的记录，`migrate_to_db.py` 只能靠 Python 内存比对去重，既低效又不可靠。

**修复：** 在 `init_db` 中先对存量数据去重，再建唯一索引，并将所有 INSERT 改为 `INSERT OR IGNORE`。

```python
def init_db():
    with closing(_get_conn()) as conn:
        cursor = conn.cursor()
        # ... CREATE TABLE ...

        # 先对存量重复数据去重（保留 id 最小的那条）
        cursor.execute(
            "DELETE FROM plate_records WHERE id NOT IN ("
            "  SELECT MIN(id) FROM plate_records"
            "  GROUP BY video_name, timestamp, plate_no"
            ")"
        )
        cursor.execute(
            "DELETE FROM rush_events WHERE id NOT IN ("
            "  SELECT MIN(id) FROM rush_events"
            "  GROUP BY timestamp, plate_no, warning_type"
            ")"
        )
        # 建唯一索引，后续 INSERT OR IGNORE 自动防重
        cursor.execute(
            "CREATE UNIQUE INDEX IF NOT EXISTS uq_plate_records "
            "ON plate_records(video_name, timestamp, plate_no)"
        )
        cursor.execute(
            "CREATE UNIQUE INDEX IF NOT EXISTS uq_rush_events "
            "ON rush_events(timestamp, plate_no, warning_type)"
        )
        conn.commit()

# INSERT 全部改为 INSERT OR IGNORE
conn.execute(
    "INSERT OR IGNORE INTO plate_records (video_name, timestamp, plate_no) VALUES (?, ?, ?)",
    ...
)
```

---

### 1.3 新增实时单条写入接口

**问题：** 原来只有 `insert_plate_records`（批量），导致主程序只能在视频结束时一次性写库，程序崩溃会丢失整段视频的过车记录。

```python
# ✅ 新增：实时写入单条记录
def insert_plate_record(video_name: str, timestamp: str, plate_no: str) -> None:
    """实时写入单条过车记录，重复则忽略。"""
    with closing(_get_conn()) as conn:
        conn.execute(
            "INSERT OR IGNORE INTO plate_records (video_name, timestamp, plate_no) VALUES (?, ?, ?)",
            (os.path.basename(video_name), timestamp, plate_no),
        )
        conn.commit()
```

---

### 1.4 新增更新与搜索接口

**问题：** `db.py` 没有 UPDATE 函数，导致 `modify_plate_number` 修改车牌时数据库始终是旧值；也没有搜索接口，无法实现按车牌/类型过滤查询。

```python
# ✅ 新增：更新车牌记录
def update_plate_record(old_plate: str, new_plate: str, timestamp: str, video_name: str) -> int:
    with closing(_get_conn()) as conn:
        cursor = conn.execute(
            "UPDATE plate_records SET plate_no = ? "
            "WHERE video_name = ? AND timestamp = ? AND plate_no = ?",
            (new_plate, os.path.basename(video_name), timestamp, old_plate),
        )
        conn.commit()
        return cursor.rowcount

# ✅ 新增：更新冲岗事件中的车牌
def update_rush_plate(old_plate: str, new_plate: str) -> int:
    with closing(_get_conn()) as conn:
        cursor = conn.execute(
            "UPDATE rush_events SET plate_no = ? WHERE plate_no = ?",
            (new_plate, old_plate),
        )
        conn.commit()
        return cursor.rowcount

# ✅ 新增：带过滤的过车记录查询
def fetch_plate_records(limit=1000, plate_no=None, video_name=None):
    ...

# ✅ 新增：fetch_rush_events 支持过滤参数
def fetch_rush_events(limit=500, plate_no=None, warning_type=None):
    ...
```

---

## 二、`main.py` 修复（16 项）

### 2.1 修复 Levenshtein 去抖逻辑顺序错误（严重 Bug）

**问题：** 原代码先 `vehicle_count += 1`，再检测相似度，若相似则调 `handle_plate_duplicate()` 删除 `passed_plates[-1]`，但此时当前车牌**还没 append**，删掉的是上一辆车的记录，随后当前（近似）车牌照样被追加进去——去抖逻辑完全反了。

```python
# ❌ 修复前：顺序错误，删的是上一辆车
def update_plate_info(self, plate_no):
    self.vehicle_count += 1                    # 1. 先加计数
    distance = self.calculate_levenshtein_distance(plate_no)
    if 0 < distance < 3:
        self.handle_plate_duplicate()          # 2. 删 passed_plates[-1]（上一辆！）
    ...
    self.passed_plates.append((ts, plate_no)) # 3. 再把当前车牌加进去

# ✅ 修复后：先判断，相似直接 return，不影响任何已有记录
def _on_plate_detected(self, plate_no: str):
    if not plate_no:
        return
    if plate_no in self._passed_plate_set:    # O(1) 精确去重
        return
    # 先判断是否与上一辆相似，再决定是否追加
    if self.previous_plate and 0 < Levenshtein.distance(plate_no, self.previous_plate) < 3:
        logger.debug(f"去抖丢弃: {plate_no}")
        return                                 # 直接丢弃，不动任何状态
    # 确认为新车辆，再追加
    self.passed_plates.append((ts, plate_no))
    self._passed_plate_set.add(plate_no)
    self.vehicle_count += 1
```

---

### 2.2 修复车杆状态处理逻辑（严重 Bug）

**问题：** 原代码先更新 `previous_rod_state`，再判断 `if previous == current`，该条件永远为 True，导致 `handle_rod_state_no_change` 每次 YOLO 检测都执行，包括状态刚刚改变的那一帧。

```python
# ❌ 修复前：条件永远为 True
self.previous_rod_state = current_rod_state   # 先更新
if self.previous_rod_state == current_rod_state:  # 永远 True！
    self.handle_rod_state_no_change(current_rod_state)

# ✅ 修复后：明确 if/else 分支，赋值移到分支判断之后
if rod_state != self.previous_rod_state:
    self._handle_rod_state_change(rod_state)   # 仅状态变化时触发
self._handle_rod_state_no_change(rod_state)    # 每次检测都执行（明确语义）
self.previous_rod_state = rod_state
```

---

### 2.3 修复 `check_for_rush` 追加空字符串（严重 Bug）

**问题：** 程序启动时 `self.current_plate = ""`，车杆抬起时若未识别到车牌就调用 `check_for_rush`，空字符串被追加到 `current_up_plates`，导致第一辆真实车辆进来时 `len > 1`，误触发跟车冲岗警报。

```python
# ❌ 修复前：无守卫，空字符串被追加
def check_for_rush(self):
    if self.current_plate not in self.current_up_plates:
        self.current_up_plates.append(self.current_plate)  # '' 被追加

# ✅ 修复后：加守卫
def _check_for_rush(self) -> bool:
    if not self.current_plate:          # 空车牌直接返回
        return False
    if self.current_plate not in self.current_up_plates:
        self.current_up_plates.append(self.current_plate)
    ...
```

---

### 2.4 修复 `record_rush_event` 额外消耗一帧（严重 Bug）

**问题：** 触发冲岗时调用 `self.cap.read()` 读取下一帧用于截图，这帧从视频流中消失，既不显示也不做检测，造成帧序列断裂，且截图内容是事件发生后的那帧而非当前帧。

```python
# ❌ 修复前：额外读一帧，打乱视频流
def record_rush_event(self, ...):
    ret, frame = self.cap.read()    # 消耗了视频流中的下一帧！
    if ret:
        cv2.imwrite(image_path, frame)

# ✅ 修复后：使用 Worker 线程传来的最新帧
def _on_frame_ready(self, frame, rod_state):
    self.latest_frame = frame       # 每帧更新最新帧缓存

def _record_rush_event(self, ...):
    if self.latest_frame is not None:
        cv2.imwrite(image_path, self.latest_frame)   # 直接用缓存，不读流
```

---

### 2.5 修复 `modify_plate_number` 不更新数据库

**问题：** 修改车牌只更新了内存和 txt 文件，SQLite 数据库中的记录始终是旧车牌号，造成数据不一致。

```python
# ❌ 修复前：只改内存和 txt，数据库被遗忘
def modify_plate_number(self):
    self.passed_plates[row] = (ts, new_plate)
    with open(PLATE_RECORD_PATH, "a") as f:
        f.write(...)
    # 数据库从未更新！

# ✅ 修复后：同时更新数据库和内存冲岗列表
def modify_plate_number(self):
    self.passed_plates[row] = (ts, new_plate)
    self._passed_plate_set.discard(old_plate)
    self._passed_plate_set.add(new_plate)
    # 更新数据库
    n1 = update_plate_record(old_plate, new_plate, ts, self.current_video_path)
    n2 = update_rush_plate(old_plate, new_plate)
    # 同步内存冲岗列表
    for ev in self.rush_event_list:
        if ev.get("plate") == old_plate:
            ev["plate"] = new_plate
```

---

### 2.6 修复 `open_file_dialog` 先清状态再弹窗

**问题：** 原代码先调 `reset_status()` 清空所有数据，再弹文件对话框。用户取消选择时，当前视频的过车记录已被清空且无法恢复。

```python
# ❌ 修复前：取消对话框也会丢失数据
def open_file_dialog(self):
    self.reset_status()                        # 先清空
    file_path, _ = QFileDialog.getOpenFileName(...)
    if file_path:                              # 取消则数据已丢失
        self.load_video(file_path)

# ✅ 修复后：确认选择后再清空
def open_file_dialog(self):
    path, _ = QFileDialog.getOpenFileName(...)
    if not path:
        return                                 # 取消则原数据保留
    self._reset_state()                        # 确认后再清空
    self._start_capture(cv2.VideoCapture(path), path)
```

---

### 2.7 修复 config 加载失败导致 KeyError 崩溃

**问题：** `config.json` 解析失败时返回 `{}`，后续 `self.config["model_path"]` 直接抛出 `KeyError` 使程序崩溃，无任何提示。

```python
# ❌ 修复前：返回空字典，后续 KeyError 崩溃
except Exception as e:
    logger.error(...)
    return {}   # self.config["model_path"] → KeyError!

# ✅ 修复后：始终与默认值合并，保证所有 key 存在
_DEFAULTS = {
    "model_path":           "best.pt",
    "font_path":            "simhei.ttf",
    "confidence_threshold": 0.975,
    "alert_sound":          "alert.wav",
}

def load_config() -> dict:
    config = dict(_DEFAULTS)          # 先用默认值初始化
    try:
        with open(CONFIG_PATH, "r", encoding="utf-8") as f:
            config.update(json.load(f))   # 用文件内容覆盖
    except Exception as e:
        logger.error(f"加载配置文件失败: {e}，使用默认配置")
    return config                     # 始终返回完整字典
```

---

### 2.8 推理移入后台线程，解决 UI 卡顿

**问题：** YOLO 推理（~41ms）和 HyperLPR3 识别均在 Qt 主线程执行，期间整个 UI 冻结，按钮无响应。

**修复：** 新增 `VideoWorker(QThread)` 类，在后台线程完成帧读取和所有推理，通过 Qt 信号将结果回传主线程。

```python
class VideoWorker(QThread):
    frame_ready    = pyqtSignal(object, object)   # (frame_bgr, rod_state)
    plate_detected = pyqtSignal(str)              # 识别到的车牌号
    video_finished = pyqtSignal()

    def run(self):
        frame_count = 0
        while self._running:
            if self._paused:
                self.msleep(30)
                continue
            ret, frame = self.cap.read()
            if not ret:
                self.video_finished.emit(); break

            rod_state = None
            if frame_count % 10 == 0:            # YOLO 检测车杆
                results = self.yolo_model(frame, verbose=False)[0]
                ...

            if frame_count % 7 == 0:             # LPR 识别车牌
                lpr_results = self.plate_catcher(frame)
                ...
                self.plate_detected.emit(plate_no)

            self.frame_ready.emit(frame.copy(), rod_state)
            frame_count += 1
            self.msleep(self._interval_ms)        # 按视频帧率休眠

# 主窗口通过信号槽接收结果，UI 全程不阻塞
self.worker.frame_ready.connect(self._on_frame_ready)
self.worker.plate_detected.connect(self._on_plate_detected)
self.worker.video_finished.connect(self._on_video_finished)
```

---

### 2.9 修复视频播放速度不匹配帧率

**问题：** 原 `QTimer` 固定 30ms 触发（≈33fps），监控视频通常是 25fps（40ms/帧），导致视频以约 1.3× 速度快进播放，时间戳与视频内容不同步。

```python
# ❌ 修复前：固定 30ms，忽视视频实际帧率
self.timer.start(30)

# ✅ 修复后：Worker 根据视频 FPS 自动计算帧间隔
fps = cap.get(cv2.CAP_PROP_FPS)
self._interval_ms = max(1, int(1000 / fps)) if fps > 0 else 40
# 如 25fps → 40ms，30fps → 33ms，摄像头未知 → 40ms
self.msleep(self._interval_ms)
```

---

### 2.10 修复字体每帧重新加载

**问题：** `draw_chinese_text_pil` 每次调用都执行 `ImageFont.truetype(font_path, font_size)` 从磁盘读取字体文件，每 7 帧一次，持续 I/O 开销。

```python
# ❌ 修复前：每帧读磁盘
def draw_chinese_text_pil(image, text, position, font_path, font_size=30, ...):
    font = ImageFont.truetype(font_path, font_size)   # 每次打开文件

# ✅ 修复后：模块级字典缓存
_font_cache: dict = {}

def _get_font(font_path: str, size: int = 30) -> ImageFont.FreeTypeFont:
    key = (font_path, size)
    if key not in _font_cache:
        _font_cache[key] = ImageFont.truetype(font_path, size)   # 只读一次
    return _font_cache[key]
```

---

### 2.11 修复车牌查重 O(n) 线性搜索

**问题：** 每次识别车牌都构建完整列表 `[p[1] for p in self.passed_plates]` 再线性搜索，车辆多时性能线性下降。

```python
# ❌ 修复前：O(n) 每次构建列表搜索
if plate_no not in [p[1] for p in self.passed_plates] and plate_no != self.previous_plate:

# ✅ 修复后：维护 set，O(1) 查重
self._passed_plate_set: set = set()        # reset 时清空
self._passed_plate_set.add(plate_no)       # 新车牌时加入

if plate_no in self._passed_plate_set:     # O(1)
    return
```

---

### 2.12 修复无意义变量名

**问题：** `self.a`、`self.b` 完全无法表达含义，维护困难。

```python
# ❌ 修复前
self.a = False   # 代表撞杆冲岗是否已记录
self.b = False   # 代表跟车冲岗是否已记录

# ✅ 修复后
self.barrier_crash_recorded = False   # 撞杆冲岗已记录标志
self.tailgate_recorded      = False   # 跟车冲岗已记录标志
```

---

### 2.13 修复系统日志可被 UI 覆写

**问题：** "数据统计 → 系统日志"页的文本框可编辑，且有"保存"按钮，用户可以任意篡改或清空日志文件。

```python
# ❌ 修复前：日志可编辑
self.log_edit = QTextEdit()              # 可编辑
self.btn_save_log = QPushButton("保存系统日志修改")

# ✅ 修复后：只读，移除保存按钮
self.log_edit = QTextEdit()
self.log_edit.setReadOnly(True)          # 禁止编辑
# btn_save_log 已移除
```

---

### 2.14 修复警告标签 `\n` 不换行

**问题：** `QLabel` 默认不渲染 `\n` 为换行，`"警告：撞杆冲岗 \n可疑车牌：浙JH035F"` 显示为一行。

```python
# ❌ 修复前：\n 不换行
self.warning_label = QLabel()
self.warning_label.setText(f"警告：撞杆冲岗 \n可疑车牌：{plate}")  # 单行显示

# ✅ 修复后：启用自动换行
self.warning_label = QLabel()
self.warning_label.setWordWrap(True)     # \n 正确换行
```

---

### 2.15 修复 `QFileSystemWatcher` 重复注册

**问题：** `init_ui` 中对同一批路径调用了三次 `addPath` / `addPaths`，每次文件变化触发多次回调。

```python
# ❌ 修复前：三处注册，存在重叠
self.file_watcher.addPath(RUSH_EVENT_RECORD)               # 第1次
self.file_watcher.addPaths([..., RUSH_EVENT_RECORD])       # 第2次（重复）
self.file_watcher.addPaths([PLATE_RECORD_PATH, LOG_PATH, CONFIG_PATH])  # 第3次

# ✅ 修复后：__init__ 末尾一次注册
self.file_watcher.addPaths([LOG_PATH, CONFIG_PATH, RUSH_EVENT_RECORD])
```

---

### 2.16 修复窗口固定尺寸

**问题：** `setFixedSize(1800, 1188)` 在 1080p 屏幕上超出屏幕高度，底部控件不可见，且禁止了窗口缩放。

```python
# ❌ 修复前：硬编码固定尺寸
self.setFixedSize(1800, 1188)

# ✅ 修复后：设置最小尺寸，初始大小可调整
self.setMinimumSize(1280, 800)
self.resize(1800, 1100)
```

---

## 三、`migrate_to_db.py` 修复（1 项）

### 3.1 改用 `db.py` 统一接口

**问题：** 直接使用裸 `sqlite3.connect()`，绕过了 `db.py` 所有封装，手动 Python 去重低效，且若中途异常连接不会被关闭。

```python
# ❌ 修复前：绕过 db.py，手动管理连接
conn = sqlite3.connect(DB_PATH)
cursor = conn.cursor()
cursor.execute("SELECT ... FROM plate_records")
existing = set(cursor.fetchall())   # 全量加载到内存去重
...
conn.close()

# ✅ 修复后：全部使用 db.py 接口，INSERT OR IGNORE 自动去重
from db import init_db, insert_plate_records, insert_rush_event

def migrate():
    init_db()           # 建表 + 唯一索引
    plate_rows = parse_plate_records(PLATE_RECORD_PATH)
    rush_rows  = parse_rush_events(RUSH_EVENT_RECORD)
    # 按视频分组批量写入，重复自动忽略
    for video_name, records in by_video.items():
        insert_plate_records(video_name, records)
    for item in rush_rows:
        insert_rush_event(event, item["video_name"])
```

---

## 四、新增功能

### 4.1 摄像头实时接入

```python
def open_camera(self):
    idx, ok = QInputDialog.getInt(self, "打开摄像头", "摄像头编号（通常为 0）：", 0, 0, 10)
    if not ok: return
    cap = cv2.VideoCapture(idx)
    if not cap.isOpened():
        QMessageBox.critical(self, "错误", f"无法打开摄像头 {idx}")
        return
    self._reset_state()
    self._start_capture(cap, f"camera_{idx}")
```

### 4.2 车牌查询页（数据库搜索）

将原来的纯文本 `plate_records.txt` 显示替换为可交互的 `QTableWidget`，支持按车牌号模糊搜索：

- 输入关键字 → 点击"搜索"→ 调用 `fetch_plate_records(plate_no=keyword)`
- 点击"刷新"→ 显示最新 2000 条记录

### 4.3 冲岗情况搜索过滤

在冲岗列表上方新增搜索栏：

- 输入车牌号关键字
- 下拉选择事件类型（全部 / 撞杆冲岗 / 跟车冲岗）
- 组合过滤，调用 `fetch_rush_events(plate_no=..., warning_type=...)`

### 4.4 过车记录实时写库

```python
# ✅ 识别到车牌立即写库，不再等视频结束
def _on_plate_detected(self, plate_no: str):
    ...
    try:
        insert_plate_record(self.current_video_path, timestamp, plate_no)
    except Exception as e:
        logger.error(f"车牌实时写库失败: {e}")
```

即使程序中途崩溃，已识别的车牌也不会丢失。

---

## 五、修复问题汇总

| 优先级 | 文件 | 问题描述 | 状态 |
|:---:|---|---|:---:|
| 🔴 严重 | main.py | Levenshtein 去抖逻辑反，删错记录 | ✅ 已修复 |
| 🔴 严重 | main.py | `handle_rod_state_no_change` 每帧触发 | ✅ 已修复 |
| 🔴 严重 | main.py | `check_for_rush` 追加空字符串误报 | ✅ 已修复 |
| 🔴 严重 | main.py | `record_rush_event` 额外消耗一帧 | ✅ 已修复 |
| 🔴 严重 | main.py | `modify_plate_number` 不更新数据库 | ✅ 已修复 |
| 🔴 严重 | main.py | `open_file_dialog` 取消后数据丢失 | ✅ 已修复 |
| 🔴 严重 | main.py | config 失败返回 `{}` → KeyError 崩溃 | ✅ 已修复 |
| 🟠 中等 | db.py | SQLite 连接泄漏 | ✅ 已修复 |
| 🟠 中等 | db.py | 无唯一约束，重复数据无法防止 | ✅ 已修复 |
| 🟠 中等 | db.py | 缺少 update / search 接口 | ✅ 已修复 |
| 🟠 中等 | main.py | 过车记录只在视频结束才写库 | ✅ 已修复 |
| 🟡 轻微 | main.py | 字体每帧重新读磁盘 | ✅ 已修复 |
| 🟡 轻微 | main.py | 车牌查重 O(n) 线性搜索 | ✅ 已修复 |
| 🟡 轻微 | main.py | 主线程推理导致 UI 卡顿 | ✅ 已修复 |
| 🟡 轻微 | main.py | 视频播放速度不匹配帧率 | ✅ 已修复 |
| 🟡 轻微 | main.py | `self.a`/`self.b` 命名无意义 | ✅ 已修复 |
| 🟡 轻微 | main.py | 系统日志可被 UI 任意覆写 | ✅ 已修复 |
| 🟡 轻微 | main.py | `\n` 在 QLabel 不换行 | ✅ 已修复 |
| 🟡 轻微 | main.py | `QFileSystemWatcher` 路径重复注册 | ✅ 已修复 |
| 🟡 轻微 | main.py | 窗口固定尺寸在小屏幕超出边界 | ✅ 已修复 |
| 🔵 架构 | migrate_to_db.py | 绕过 db.py 直接使用裸 sqlite3 | ✅ 已修复 |
| ⭐ 新增 | main.py | 摄像头实时接入 | ✅ 已实现 |
| ⭐ 新增 | main.py | 车牌查询/冲岗情况数据库搜索 | ✅ 已实现 |
