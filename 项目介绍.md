# 基于机器视觉的高速收费站卡口冲岗行为识别系统

## 一、项目背景与价值

高速公路收费站存在车辆未缴费强行闯卡（"冲岗"）的问题，传统人工监控效率低、响应慢，且难以留存有效证据。本项目通过计算机视觉与深度学习技术，实现对收费站卡口的**实时自动监控**，自动识别冲岗行为，触发声音报警并截图存档，为事后取证和交通执法提供支持。

---

## 二、技术栈

| 层次 | 技术 | 版本 | 用途 |
|---|---|---|---|
| **界面层** | PyQt5 | 5.15.9 | 桌面 GUI 框架，含 QThread 多线程 |
| **目标检测** | YOLOv8n（Ultralytics） | 8.3.78 | 检测收费杆状态（抬起/关闭/损坏） |
| **车牌识别** | HyperLPR3 | 0.1.3 | 中文车牌端到端识别 |
| **图像处理** | OpenCV | 4.11 | 视频/摄像头解码、帧处理、截图保存 |
| **中文渲染** | Pillow（PIL） | 11.1 | 在视频帧上绘制中文车牌标注 |
| **字符去抖** | python-Levenshtein | 0.26 | 编辑距离去除同一辆车的重复识别 |
| **推理加速** | PyTorch + CUDA 11.8 | 2.6.0 | GPU 加速 YOLO 推理（RTX 3050） |
| **持久化** | SQLite（sqlite3） | 内置 | 车牌记录与冲岗事件结构化存储 |
| **日志** | Python logging | 内置 | 按天轮转，保留 7 天 |
| **报警** | Pygame | 2.6.1 | 播放报警音频 |
| **运行环境** | Python 3.11 + Windows | — | — |

---

## 三、系统架构

### 3.1 整体架构（优化后）

```
视频文件 / 摄像头
        │
        ▼
  VideoWorker（QThread 后台线程）
        │
        ├─── 每 10 帧 ──► YOLOv8n（GPU CUDA）──► 车杆状态
        │                                          ├── 抬起   → 清空本次开闸车辆列表
        │                                          ├── 关闭   → 检查是否跟车冲岗
        │                                          └── 损坏   → 触发【撞杆冲岗】警报
        │
        ├─── 每 7 帧 ───► HyperLPR3 ────────────► 车牌号 + 置信度
        │                    │
        │                    └── 置信度 ≥ 0.975
        │                        → Levenshtein 去抖（编辑距离 < 3 丢弃）
        │                        → set O(1) 精确去重
        │                        → 实时写入 SQLite
        │
        ├─── 每帧 ──────► 缓存 latest_frame（用于冲岗截图）
        │
        └─── Qt 信号 ───► 主线程（UI 更新 / 冲岗检测逻辑）
                               │
                               ▼
              截图 + JSON 存档 + SQLite 写库 + 声音报警 + 表格刷新
```

### 3.2 架构演进说明

| 版本 | 架构方式 | 问题 |
|---|---|---|
| 初版 | QTimer 驱动，主线程执行 YOLO+LPR | UI 每帧冻结 ~60ms，按钮无响应 |
| 优化后 | `VideoWorker(QThread)` 后台推理，信号回传主线程 | UI 全程流畅，推理与显示解耦 |

---

## 四、冲岗检测逻辑

### 4.1 跟车冲岗
> 一次车杆抬起期间，通过了超过 1 辆不同车辆

```python
def _check_for_rush(self) -> bool:
    if not self.current_plate:           # 空车牌守卫，防误报
        return False
    if self.current_plate not in self.current_up_plates:
        self.current_up_plates.append(self.current_plate)
    if len(self.current_up_plates) > 1 and self.vehicle_count > 1:
        触发跟车冲岗警报
        return True
    return False
```

### 4.2 撞杆冲岗
> YOLO 检测到车杆状态变为"损坏（unknown）"，且当前有车辆正在通过

```python
def _handle_rod_state_no_change(self, state: str):
    if state == "车杆损坏":
        if self.current_plate and not self.barrier_crash_recorded:
            触发撞杆冲岗警报
            self.barrier_crash_recorded = True   # 同一事件只记录一次
```

### 4.3 防重复触发

用语义清晰的布尔标志位控制，每次开闸周期开始时重置：

```python
self.barrier_crash_recorded = False   # 撞杆冲岗已记录（原 self.a）
self.tailgate_recorded      = False   # 跟车冲岗已记录（原 self.b）
```

---

## 五、YOLOv8 训练过程

### 5.1 模型选型
选用 **YOLOv8n（Nano）**：
- 参数量：**3.0M**，模型文件仅 **5.9 MB**
- RTX 3050 推理约 **41ms/帧**，适合桌面端实时部署
- 3 个检测类别：`up`（抬起）、`down`（关闭）、`unknown`（损坏）

### 5.2 数据集准备

1. **数据来源**：从高速收费站实际监控视频中抽帧
2. **标注工具**：使用 [Labelimg](https://github.com/HumanSignal/labelImg) 进行 YOLO 格式标注（`.txt`，每行 `class cx cy w h`）
3. **数据集划分**：训练集 / 验证集 / 测试集 = 7:2:1
4. **目录结构**：
```
dataset/
├── images/
│   ├── train/
│   └── val/
└── labels/
    ├── train/
    └── val/
```
5. **`data.yaml` 配置**：
```yaml
path: ./dataset
train: images/train
val:   images/val
nc: 3
names: ['up', 'down', 'unknown']
```

### 5.3 训练命令

```bash
yolo detect train \
  data=data.yaml \
  model=yolov8n.pt \
  epochs=100 \
  imgsz=640 \
  batch=16 \
  device=0 \
  project=runs/train \
  name=tollgate_rod
```

关键超参数说明：
- `epochs=100`：收敛后 Early Stopping 自动停止
- `imgsz=640`：输入分辨率，平衡精度与速度
- `batch=16`：根据显存调整（4GB 建议 8~16）
- `device=0`：使用第 0 块 GPU

### 5.4 训练结果评估

训练完成后查看 `runs/train/tollgate_rod/` 下的指标：
- **mAP@0.5**：主要评估指标，目标 ≥ 0.90
- **Precision / Recall**：查看各类别是否均衡
- `best.pt` 为验证集 mAP 最高权重

### 5.5 为什么选 YOLOv8 而不是传统方法？

| 对比项 | 传统图像处理（背景差分、帧差法） | YOLOv8 |
|---|---|---|
| 光照适应性 | 差（夜间、阴雨效果差） | 强（训练集覆盖多场景） |
| 遮挡处理 | 弱 | 强 |
| 类别语义 | 无（只检测运动区域） | 直接输出"抬起/关闭/损坏" |
| 部署复杂度 | 低 | 中（需要 GPU 或 ONNX 转换） |

---

## 六、数据库设计

### 6.1 为什么用 SQLite？
- 嵌入式数据库，**零配置、零部署**，单文件 `system_data.db`
- 支持标准 SQL，方便后期迁移到 MySQL/PostgreSQL
- 适合单机桌面应用，无需网络连接

### 6.2 表结构

**`plate_records`** — 所有过车记录
```sql
CREATE TABLE plate_records (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    video_name TEXT,
    timestamp  TEXT NOT NULL,                  -- YYYY-MM-DD HH:MM:SS
    plate_no   TEXT NOT NULL,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(video_name, timestamp, plate_no)    -- 唯一约束防重复写入
);
```

**`rush_events`** — 冲岗报警记录
```sql
CREATE TABLE rush_events (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp    TEXT NOT NULL,
    plate_no     TEXT NOT NULL,
    warning_type TEXT NOT NULL,                -- 撞杆冲岗 / 跟车冲岗
    message      TEXT NOT NULL,
    image_path   TEXT,                         -- 截图文件路径
    video_name   TEXT,
    created_at   TEXT DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(timestamp, plate_no, warning_type)  -- 唯一约束防重复写入
);
```

### 6.3 索引设计

```sql
-- 普通索引：加速按时间/车牌查询
CREATE INDEX idx_plate_timestamp ON plate_records(timestamp);
CREATE INDEX idx_plate_no        ON plate_records(plate_no);
CREATE INDEX idx_rush_timestamp  ON rush_events(timestamp);
CREATE INDEX idx_rush_plate_no   ON rush_events(plate_no);

-- 唯一索引：INSERT OR IGNORE 自动去重
CREATE UNIQUE INDEX uq_plate_records ON plate_records(video_name, timestamp, plate_no);
CREATE UNIQUE INDEX uq_rush_events   ON rush_events(timestamp, plate_no, warning_type);
```

### 6.4 数据流（优化后）

```
检测到车牌（实时）
    └─► insert_plate_record(video_name, timestamp, plate_no)
            └─► INSERT OR IGNORE 立即写入（不再等视频结束）

检测到冲岗（实时）
    └─► insert_rush_event(event_dict, video_name)
            └─► INSERT OR IGNORE 立即写入

UI 数据统计页搜索
    └─► fetch_plate_records(plate_no="关键字")   ← 支持模糊搜索
    └─► fetch_rush_events(warning_type="撞杆冲岗") ← 支持类型过滤
```

### 6.5 数据库演进过程

```
阶段一：纯文件存储
    plate_records.txt（追加写入）
    rush_records.json（全量覆写）
         │
         │ 问题：无法查询，崩溃丢数据，格式脆弱
         ▼
阶段二：引入 SQLite（当前版本）
    system_data.db
    ├── plate_records（唯一索引 + 实时写入）
    └── rush_events  （唯一索引 + 实时写入）

    migrate_to_db.py 一键迁移历史数据（幂等，可重复执行）
```

---

## 七、关键技术难点与解决方案

### 难点 1：同一辆车多帧重复识别

**问题**：LPR 每 7 帧运行一次，同一辆车在镜头内时会被连续识别多次，且同一车牌可能因 OCR 误差识别为略有不同的字符串（如"鄂ADZ5756"和"鄂ADZ5757"）。

**解决（两层去重）**：
```python
# 第一层：set O(1) 精确去重（完全相同的车牌）
if plate_no in self._passed_plate_set:
    return

# 第二层：Levenshtein 模糊去抖（OCR 误差，编辑距离 1~2）
if 0 < Levenshtein.distance(plate_no, self.previous_plate) < 3:
    return   # 丢弃，不影响已有记录
```

> **注意**：去抖逻辑的顺序至关重要——必须先判断再追加，否则会删除上一辆正确记录。

### 难点 2：中文字符叠加到视频帧

**问题**：OpenCV 的 `putText` 不支持中文，会显示乱码。

**解决**：BGR → PIL Image → `ImageDraw.text(simhei.ttf)` → 转回 BGR，同时用字典缓存字体对象避免每帧读磁盘。

```python
_font_cache: dict = {}

def _get_font(font_path, size=30):
    key = (font_path, size)
    if key not in _font_cache:
        _font_cache[key] = ImageFont.truetype(font_path, size)  # 只读一次
    return _font_cache[key]
```

### 难点 3：推理阻塞 UI 主线程

**问题**：YOLO 推理约 41ms，LPR 约 20ms，全在主线程执行时 UI 每帧冻结约 60ms，用户交互明显卡顿。

**解决**：将帧读取与推理完整迁移到 `VideoWorker(QThread)`，通过 Qt 信号将结果安全地回传到主线程更新 UI，两者完全解耦。

### 难点 4：截图不能额外消耗视频帧

**问题**：冲岗事件触发时需要截图，原来用 `cap.read()` 读取下一帧，导致该帧从视频流消失，帧序列断裂。

**解决**：在每帧处理时缓存 `self.latest_frame`，截图时直接使用缓存，不再从流中额外读取。

### 难点 5：SQLite 连接资源管理

**问题**：`with conn:` 只管事务提交/回滚，不关闭连接，频繁调用会积累大量未关闭的文件句柄。

**解决**：所有数据库操作统一使用 `contextlib.closing()`：

```python
with closing(_get_conn()) as conn:   # 退出时自动关闭连接
    conn.execute(...)
    conn.commit()
```

---

## 八、项目迭代亮点（面试重点）

> 这一部分体现了从"能跑"到"工程质量"的提升过程，是面试中展示**工程能力**的核心内容。

### 8.1 发现并修复 7 个逻辑 Bug

| Bug | 现象 | 根本原因 | 修复方案 |
|---|---|---|---|
| Levenshtein 去抖 | 正常车辆被误删 | append 在 delete 之后，删的是上一辆 | 先判断再追加，相似直接 return |
| 车杆状态处理 | `no_change` 每帧触发 | 赋值后判断，条件永远 True | 改为 if/else，赋值移后 |
| 空车牌误报 | 启动即触发跟车冲岗 | `current_plate=""` 被追加到列表 | 加 `if not self.current_plate` 守卫 |
| 截图帧错位 | 截图内容偏后一帧 | `cap.read()` 消耗了流中下一帧 | 缓存 `latest_frame`，不再读流 |
| 修改车牌数据不一致 | 数据库仍是旧车牌 | 只改了内存和 txt，漏掉 DB | 调用 `update_plate_record` + `update_rush_plate` |
| 取消对话框丢数据 | 数据意外清空 | reset 在弹框之前 | 确认选择后再 reset |
| 配置加载崩溃 | `KeyError` 闪退 | 失败返回 `{}`，后续 key 访问崩溃 | 与默认值合并，保证所有 key 存在 |

### 8.2 性能优化

- **推理不再阻塞 UI**：QThread 多线程，主线程纯粹负责显示
- **视频速度与帧率匹配**：读取 `CAP_PROP_FPS` 动态计算帧间隔（原固定 30ms）
- **字体缓存**：从每帧读磁盘改为模块级字典缓存，I/O 次数降为 1
- **O(1) 车牌查重**：从 `list` 线性搜索改为 `set` 哈希查找

### 8.3 数据库工程化

- 添加 `UNIQUE INDEX` + `INSERT OR IGNORE`，从根本上防止重复数据
- 过车记录从"视频结束批量写入"改为"识别即实时写入"，消除崩溃丢数据风险
- 新增 `update_plate_record`、`update_rush_plate`、`fetch_plate_records` 等接口，数据操作完整闭环
- 所有连接用 `contextlib.closing()` 管理，彻底消除连接泄漏

### 8.4 新增功能

- **摄像头实时接入**：支持 `cv2.VideoCapture(index)` 接入 USB/网络摄像头
- **数据检索**：车牌查询页支持模糊搜索；冲岗情况页支持按车牌+类型组合过滤
- **数据库搜索接口**：`fetch_plate_records(plate_no=...)` / `fetch_rush_events(warning_type=...)`

---

## 九、项目亮点总结（面试一分钟版）

> 面试开场可直接使用以下表述：

**"这个项目是一个基于机器视觉的收费站冲岗识别桌面系统。核心用 YOLOv8n 检测收费杆状态、HyperLPR3 识别车牌，判断跟车和撞杆两种冲岗行为，触发实时报警并截图入库。**

**技术上有几个我比较关注的点：第一，推理放在了 QThread 后台线程，通过 Qt 信号回传主线程，避免 UI 卡顿；第二，车牌去重用了两层机制——set 做精确去重、Levenshtein 编辑距离做 OCR 误差去抖，去抖顺序很关键，先判断再追加；第三，数据库这块从早期的 txt/json 文件演进到了 SQLite，加了唯一索引和 INSERT OR IGNORE 防重，过车记录从批量写入改为实时写入，消除崩溃丢数据的风险。**

**迭代过程中发现并修复了 7 个逻辑 Bug，比较典型的是车杆状态判断条件永远为 True 导致 no_change 逻辑每帧触发，以及截图时额外 cap.read() 消耗视频流帧的问题。"**

---

## 十、项目运行

```bash
# 启动主程序
python main.py

# 历史数据迁移（仅需执行一次，幂等）
python migrate_to_db.py
```

**依赖环境**：Python 3.11，CUDA 11.8，NVIDIA GPU（推荐 4GB 以上显存）
