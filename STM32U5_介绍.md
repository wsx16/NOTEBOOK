# STM32U5 物联网智能终端

---

## 一、项目自我介绍话术

### 30 秒精简版

> 我做了一个基于 STM32U575 的物联网终端项目，核心是用 FreeRTOS 实现多任务调度，同时处理 Modbus RTU 工业通信、MQTT 云端通信和指纹识别三路串口协议。架构上我把所有控制指令统一抽象为 Modbus 指令包，无论来自串口、触摸屏还是云端，都通过消息队列投递到同一个处理任务，实现了控制来源与执行逻辑的解耦。UI 方面用了 LVGL 图形库，在 240x320 的 SPI 屏上做了触摸交互界面。

### 1 分钟完整版

> 这个项目是一个基于 STM32U575（Cortex-M33）的多协议物联网终端。系统运行 FreeRTOS，有三个主要任务：Modbus 指令解析任务、LVGL 界面刷新任务、以及指纹日志打印任务。
>
> 通信方面，我实现了三路并行：USART1 走 Modbus RTU 协议，用于上位机控制 LED、蜂鸣器等外设；UART5 连接 ESP8266 WiFi 模块，通过 AT 指令实现 MQTT 发布订阅；USART3 与外挂的指纹模块通信，支持录入、识别、删除操作。
>
> 架构设计上最核心的一点是：我定义了一个统一的指令包结构体 `modbus_pack_t`，所有控制来源——不管是串口中断收到的 Modbus 帧、还是触摸屏按钮的回调函数、还是 MQTT 下发的命令——都构造成这个指令包，通过 FreeRTOS 消息队列投递到处理任务。这样做实现了三层解耦：UI 不直接操作硬件，通信层不关心控制逻辑，新增控制来源只需要构造指令包入队即可。
>
> 显示方面用了 LVGL 8.3 图形库，在 ILI9341 SPI 屏上做了多屏切换的触摸界面，包括 WiFi 配置弹窗（带虚拟键盘输入）和指纹管理界面。

---

## 二、架构设计类问题

### Q1：为什么要用消息队列，而不是直接在中断里处理？

> 有三个原因：
>
> **第一，时间约束。** 中断服务函数（ISR）要求尽快退出，不能做耗时操作。比如 WiFi 连接需要发 AT 指令等待几秒钟的响应，如果在中断里做会阻塞其他中断。
>
> **第二，线程安全。** 如果中断和主循环同时访问同一个外设（比如都在操作 GPIO），会出现竞态条件。通过队列把数据投递到任务上下文处理，天然避免了这个问题。
>
> **第三，解耦。** 中断只负责收数据和入队，业务逻辑全在任务里做。这样改业务逻辑不需要动中断代码，维护性好。
>
> 具体实现上，中断里用 `xQueueSendFromISR`（ISR 安全版本），任务里用 `xQueueReceive` 阻塞等待。

### Q2：为什么要统一成 Modbus 指令包，而不是每个模块独立处理？

> 这是一个**命令模式（Command Pattern）** 的应用。统一指令包的好处是：
>
> 1. **单一处理入口**：所有外设控制逻辑集中在 `peripheral_ctrl()` 一个函数里，方便维护和调试
> 2. **来源无关**：处理函数不需要知道这条指令是串口来的还是触摸屏来的，降低了耦合
> 3. **易扩展**：如果将来加蓝牙控制，只需要在蓝牙回调里构造指令包入队即可，不需要改任何现有代码
>
> 指令包结构很简单，只有 5 个字节：设备号、功能码、CRC、可选参数。轻量且高效。

### Q3：如果队列满了怎么办？

> 我的队列深度是 4。GUI 按钮回调用的是 `xQueueSend(queuehandle, &pack, 0)`，超时时间是 0，也就是非阻塞——如果队列满了会立刻返回失败，UI 上会显示 "Busy!" 提示用户稍后再试。
>
> 串口中断里用的是 `xQueueSendFromISR`，也是非阻塞的。如果满了这一帧就丢弃，等下一帧。这在实时控制场景中是合理的——旧的指令已经过时了，处理最新的即可。

---

## 三、FreeRTOS 类问题

### Q4：三个任务的优先级是怎么分配的？为什么这样设？

> | 任务 | 优先级 | 理由 |
> |------|--------|------|
> | modbus_parse_task | Normal | 外设控制是核心业务，必须及时响应 |
> | F0_LogTask | BelowNormal | 日志打印不紧急，可以延迟 |
> | lvgl | Low | UI 刷新对实时性要求最低，5ms 周期足够流畅 |
>
> 设计原则是：**数据处理 > 日志记录 > 界面刷新**。LVGL 优先级最低是因为即使延迟几十毫秒刷新，用户也感知不到，但如果 Modbus 指令延迟处理，会影响设备控制的实时性。

### Q5：为什么指纹日志用 StreamBuffer 而不是队列？

> **StreamBuffer 适合字节流，Queue 适合结构化数据。**
>
> 指纹模块返回的是不定长的 ASCII 字符串（比如 "Enroll OK\n"），是典型的字节流场景。如果用队列，要么一个字节一个槽位（浪费），要么事先知道长度打包（不现实）。
>
> StreamBuffer 的特点是：写端可以一次写入任意长度，读端可以逐字节读取，像管道一样使用。我在中断里用 `xStreamBufferSendFromISR` 写入，任务里逐字节读取并打印，非常适合这个场景。

### Q6：`xQueueSendFromISR` 和 `xQueueSend` 有什么区别？

> 核心区别是 **能不能阻塞**：
>
> - `xQueueSend` 可以指定超时时间，队列满时会让当前任务阻塞等待。只能在**任务上下文**使用。
> - `xQueueSendFromISR` 不能阻塞，队列满了直接返回失败。只能在**中断上下文**使用。
>
> 原因是中断不能被调度器挂起——中断本身就是最高优先级的执行流，如果阻塞了，整个系统就卡死了。
>
> 另外 `xQueueSendFromISR` 有一个 `pxHigherPriorityTaskWoken` 参数，用于判断入队操作是否唤醒了更高优先级的任务，如果是，退出中断时需要调用 `portYIELD_FROM_ISR` 立即切换上下文。

---

## 四、通信协议类问题

### Q7：Modbus CRC16 的原理是什么？

> CRC16 本质上是**多项式除法取余**：
>
> 1. 初始值设为 `0xFFFF`
> 2. 逐字节异或到 CRC 寄存器低 8 位
> 3. 每个字节循环右移 8 次，每次检查最低位：如果是 1，右移后异或多项式 `0xA001`（这是标准 Modbus 反转多项式）；如果是 0，只右移
> 4. 所有字节处理完后，CRC 寄存器的值就是校验码
>
> 发送时把 CRC 附在数据后面（小端序），接收方用相同算法计算并比对，如果不一致就说明数据在传输中出错了。

### Q8：什么是 UART 空闲中断？为什么用它来接收 Modbus 帧？

> **空闲中断**（IDLE Interrupt）在串口检测到一段时间没有新数据到来时触发。STM32 HAL 封装为 `HAL_UARTEx_ReceiveToIdle_IT`。
>
> 用它来收 Modbus 帧的原因是：Modbus RTU 协议规定帧与帧之间至少有 3.5 个字符时间的间隔。空闲中断天然对应这个间隔——当总线从"有数据"变为"空闲"时触发，说明一帧数据已经收完了。
>
> 相比定长接收（`HAL_UART_Receive_IT`），空闲中断的优势是能自适应不同长度的帧，不需要事先知道帧长。

### Q9：ESP8266 的 AT 指令交互是怎么实现的？

> 核心是 `send_AT_Cmd()` 函数，采用**发送-轮询-超时**模式：
>
> 1. 清空接收缓冲区
> 2. 通过 `HAL_UART_Transmit` 发送 AT 指令（加 `\r\n` 结尾）
> 3. 进入循环，每 50ms 检查一次缓冲区是否包含预期的应答字符串（如 "OK"、"WIFI GOT IP"）
> 4. 如果在超时时间内匹配到应答，返回 true；否则返回 false
>
> 接收端是中断驱动的：UART5 的空闲中断回调 `MQTT_Rx_Handler` 把收到的数据存入 `esp_buffer`，`send_AT_Cmd` 轮询这个缓冲区。
>
> 注意这里用了 `vTaskDelay` 而不是空循环，这样在等待期间会让出 CPU 给其他任务运行。

### Q10：WiFi 连接失败怎么处理？

> `MQTT_init()` 中有重试机制：最多尝试 10 次，每次间隔 1 秒查询 IP 地址。还加了对无效 IP（0.0.0.0）的过滤——有些路由器会先返回 STAIP 但 IP 还是 0.0.0.0，需要继续等待。
>
> 如果 10 次都没连上，不会死循环，而是打印提示信息并正常返回，系统其他功能（触摸屏、Modbus 控制）不受影响。用户可以通过触摸屏上的 WiFi Settings 弹窗手动输入 SSID 和密码重新连接。

---

## 五、LVGL GUI 类问题

### Q11：LVGL 的刷新机制是怎样的？

> LVGL 需要两个"心跳"：
>
> 1. **Tick**：TIM6 每 1ms 中断调用 `lv_tick_inc(1)`，告诉 LVGL 过去了多长时间，用于动画计算、防抖等
> 2. **Handler**：`lvgl` 任务每 5ms 调用 `lv_timer_handler()`，这是 LVGL 的主循环——检查哪些控件有变化（脏区域），只重绘变化的部分，通过显示驱动刷到屏幕上
>
> 显示驱动的工作流程：LVGL 渲染好一块缓冲区后，调用 `disp_flush` 回调，我在里面通过 SPI DMA 把像素数据发给 ILI9341。缓冲区大小是 240x10 像素（一次刷 10 行），在内存占用和刷新效率之间做了平衡。

### Q12：触摸屏是怎么和 LVGL 对接的？

> 通过 LVGL 的 Input Device 接口：
>
> 1. 注册一个 `lv_indev_drv_t` 类型的输入设备驱动
> 2. 设置回调函数 `touchpad_read`，LVGL 会周期性调用它
> 3. 在回调里通过 I2C 读取 FT6336 触摸芯片的寄存器，获取触摸状态和坐标
> 4. 如果有触摸，设置 `data->state = LV_INDEV_STATE_PR` 和坐标；否则设置 `RELEASED`
>
> FT6336 的 I2C 地址是 0x38，触摸数据在寄存器 0x02 开始的连续几个字节里。

### Q13：WiFi 配置弹窗为什么要动态创建而不是预先创建？

> 两个原因：
>
> 1. **节省内存**：LVGL 的每个控件都占 RAM，键盘控件尤其大。动态创建意味着只在需要时才占用内存，关闭后释放
> 2. **避免冲突**：键盘需要覆盖在当前屏幕上方，如果预先创建并隐藏，屏幕切换时可能带来层级管理的复杂性。动态创建在 `lv_scr_act()` 上，始终在最顶层

---

## 六、综合/挑战类问题

### Q14：这个项目你遇到过最大的困难是什么？

> **建议参考方向（选一个你实际遇到的来说）：**
>
> 方向 A：MQTT AT 指令的时序问题
> > ESP8266 的 AT 指令响应时间不确定，有时候 WiFi 连接要好几秒，如果超时设太短会误判失败，设太长又会阻塞其他功能。最终我采用了 `vTaskDelay` 轮询的方式，把等待时间还给 RTOS 调度器，既不阻塞其他任务，又能及时检测到响应。
>
> 方向 B：LVGL 和 FreeRTOS 的配合
> > LVGL 不是线程安全的，不能在多个任务里同时操作 LVGL 控件。我把所有 LVGL 操作都集中在 `lvgl` 任务里，其他任务要触发 UI 变化时只通过队列传递数据，由 lvgl 任务统一处理。
>
> 方向 C：Modbus 帧边界判定
> > 串口是字节流，如何判断一帧结束？最初用定长接收经常出错。后来改用空闲中断（ReceiveToIdle），利用帧间间隔自动判定帧边界，再配合 CRC 校验保证数据完整性，问题就解决了。

### Q15：如果让你重新设计，你会做哪些改进？

> 1. **标准化 Modbus 协议**：当前是精简版 4~5 字节帧，可以扩展为标准 Modbus RTU，支持寄存器读写、多设备地址，增强通用性
> 2. **MQTT 断线重连**：加入心跳检测和自动重连机制，目前断线后需要手动重连
> 3. **完善 OTA**：把预留的 OTA 功能做完整，通过 MQTT 下发固件，实现远程升级
> 4. **加入互斥锁**：对 LVGL 操作加互斥锁保护，支持多任务安全访问 UI

### Q16：这个项目里用到了哪些中断？优先级怎么管理的？

> | 中断源 | 用途 | 说明 |
> |--------|------|------|
> | USART1 空闲中断 | Modbus 帧接收 | 一帧收完后入队 |
> | UART5 空闲中断 | ESP8266 AT 响应接收 | 写入 esp_buffer |
> | USART3 空闲中断 | 指纹模块数据接收 | 写入 StreamBuffer |
> | TIM6 更新中断 | LVGL 1ms 心跳 | 调用 lv_tick_inc(1) |
> | SysTick | FreeRTOS 系统节拍 | 1ms 任务调度 |
>
> 中断优先级方面，STM32U5 使用 NVIC 管理。FreeRTOS 要求：所有调用 FreeRTOS API（如 `xQueueSendFromISR`）的中断，其优先级必须 >= `configMAX_SYSCALL_INTERRUPT_PRIORITY`，否则会破坏 RTOS 的临界区保护。

---

## 七、加分回答技巧

1. **用"为什么"替代"是什么"**：面试官问你用了什么技术，不要只说"我用了 FreeRTOS"，要说"我用 FreeRTOS 是因为需要多任务并行处理三路串口，裸机的状态机方案会让代码耦合度太高"

2. **主动暴露取舍**：比如说"我选择了精简 Modbus 帧而不是标准帧，因为我的场景只需要简单的开关控制，标准帧的寄存器读写功能用不上，精简版可以降低解析复杂度和带宽消耗"

3. **体现调试能力**：提到你用 `printf` 调试串口数据、用逻辑分析仪抓 SPI 波形、用串口助手模拟 Modbus 主站测试等

4. **关联知识深度**：被问到 CRC 时可以扩展到"为什么 Modbus 用 0xA001 而不是 0x8005——因为 0xA001 是 0x8005 的位反转，对应的是 LSB-first 的计算方式，Modbus 采用小端序所以用反转多项式"
